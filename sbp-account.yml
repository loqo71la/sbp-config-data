server:
  port: 8085

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5435/sbp-account
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
